<!DOCTYPE html>
<html>
<head>
<meta charset="UTF-8">
<title>Payment Voucher</title>
<style>
@page { size: A4; margin: 0; }
* { margin: 0; padding: 0; box-sizing: border-box; }
body { font-family: Arial, sans-serif; font-size: 10pt; color: #000; margin: 0; padding: 0; }
.container { padding: 10px; border: 2px solid #000; }
.header { text-align: center; border-bottom: 2px solid #000; padding-bottom: 8px; margin-bottom: 12px; }
.header .title { font-size: 18pt; font-weight: bold; letter-spacing: 3px; }
.header .org { font-size: 11pt; font-weight: bold; margin-top: 4px; }
.header .address { font-size: 9pt; margin-top: 2px; }
.header img { height: 70px; margin-top: 6px; }
table { width: 100%; border-collapse: collapse; }
.info-table td { padding: 4px; vertical-align: top; }
.info-table .label { font-weight: bold; white-space: nowrap; }
.info-table .value { border-bottom: 1px dotted #666; }
.items-table { border: 2px solid #000; }
.items-table th { background: #f0f0f0; font-weight: bold; text-align: left; padding: 6px; border: 1px solid #666; }
.items-table td { padding: 6px; border: 1px solid #ccc; }
.items-table tfoot td { padding: 6px; border-top: 2px solid #000; border-right: 1px solid #666; }
.signatures-table { border: 2px solid #000; border-top: none; }
.signatures-table th { background: #f0f0f0; font-weight: bold; text-align: left; padding: 6px; border-bottom: 2px solid #000; border-right: 1px solid #666; }
.signatures-table td { padding: 10px 6px; border-bottom: 1px solid #ccc; border-right: 1px solid #666; height: 40px; }
.received-table { margin-top: 12px; }
.received-table td { padding: 4px; }
.received-table .value { border-bottom: 1px solid #000; height: 20px; }
</style>
</head>
<body>
<div class="container">

<div class="header">
    <div class="title">PAYMENT VOUCHER</div>
    <div class="org">SOIL- ANIMALS' POWER TANZANIA</div>
    <div class="address">P.O Box 149, Morogoro, Tanzania Kihonda- Kilimanjaro</div>
    <img src="data:image/png;base64,{{ base64_encode(file_get_contents(public_path('images/sapta-logo.png'))) }}">
</div>

<table class="info-table">
<tr>
    <td class="label">Date:</td>
    <td class="value">{{ $voucher->payment_date ? \Carbon\Carbon::parse($voucher->payment_date)->format('d/m/Y') : 'â€”' }}</td>
    <td class="label">Trans No:</td>
    <td class="value"><strong>{{ $voucher->trans_no ?? $voucher->voucher_number }}</strong></td>
</tr>
<tr>
    <td class="label">Name of Payee:</td>
    <td class="value">{{ $voucher->payee_name }}</td>
    <td class="label">P.O Box:</td>
    <td class="value">{{ $voucher->payee_pobox ?? 'â€”' }}</td>
</tr>
<tr>
    <td class="label">Batch:</td>
    <td class="value">{{ $voucher->batch ?? 'â€”' }}</td>
    <td class="label">Bank:</td>
    <td class="value">{{ $voucher->bank ?? 'â€”' }}</td>
</tr>
<tr>
    <td class="label">Currency:</td>
    <td class="value">{{ $voucher->currency ?? 'TZS' }}</td>
    <td class="label">Mode:</td>
    <td class="value">{{ strtoupper($voucher->mode ?? 'â€”') }}</td>
</tr>
<tr>
    <td class="label">Cheque Number:</td>
    <td class="value">{{ $voucher->cheque_number ?? 'â€”' }}</td>
    <td></td>
    <td></td>
</tr>
</table>

<table class="items-table">
<thead>
<tr>
    <th style="width:20%">Account/Invoice No</th>
    <th style="width:56%">Details</th>
    <th style="width:24%; text-align:right">Amount {{ $voucher->currency ?? 'TZS' }}</th>
</tr>
</thead>
<tbody>
@forelse($voucher->items as $item)
<tr>
    <td>{{ $item->account_invoice_no ?? 'â€”' }}</td>
    <td>{{ $item->details }}</td>
    <td style="text-align:right">{{ number_format($item->amount, 2) }}</td>
</tr>
@empty
<tr><td colspan="3" style="text-align:center;color:#999">No items</td></tr>
@endforelse
</tbody>
<tfoot>
<tr>
    <td colspan="2">
        <strong>Amount in Words:</strong><br>
        {{ $voucher->amount_in_words ?? 'â€”' }}
    </td>
    <td style="text-align:right">
        <strong>TOTAL</strong><br>
        <span style="font-size:14pt;font-weight:bold">{{ number_format($voucher->amount, 2) }}</span>
    </td>
</tr>
</tfoot>
</table>

<table class="signatures-table">
<thead>
<tr>
    <th style="width:40%">Name</th>
    <th style="width:35%">Signature</th>
    <th style="width:25%">Date</th>
</tr>
</thead>
<tbody>
<tr>
    <td><strong>Prepared By:</strong><br>{{ $voucher->preparedBy->username ?? 'â€”' }}</td>
    <td></td>
    <td>{{ $voucher->prepared_at ? \Carbon\Carbon::parse($voucher->prepared_at)->format('d/m/Y') : '' }}</td>
</tr>
<tr>
    <td><strong>Checked By:</strong><br>{{ $voucher->checkedBy->username ?? 'â€”' }}</td>
    <td></td>
    <td>{{ $voucher->checked_at ? \Carbon\Carbon::parse($voucher->checked_at)->format('d/m/Y') : '' }}</td>
</tr>
<tr>
    <td><strong>Authorized By:</strong><br>{{ $voucher->authorizedBy->username ?? 'â€”' }}</td>
    <td></td>
    <td>{{ $voucher->authorized_at ? \Carbon\Carbon::parse($voucher->authorized_at)->format('d/m/Y') : '' }}</td>
</tr>
</tbody>
</table>

<table class="received-table">
<tr>
    <td style="width:10%"><strong>Received BY:</strong></td>
    <td class="value" style="width:35%">{{ $voucher->received_by_name ?? '' }}</td>
    <td style="width:10%"></td>
    <td style="width:10%"><strong>Signature:</strong></td>
    <td class="value" style="width:35%"></td>
</tr>
</table>

</div>
</body>
</html>