<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <title>Payment Voucher {{ $voucher->trans_no ?? $voucher->voucher_number }}</title>
    <style>
        @page {
            size: A4;
            margin: 10mm;
        }
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }
        body {
            font-family: Arial, Helvetica, sans-serif;
            font-size: 11pt;
            color: #000;
            line-height: 1.4;
        }

        .container {
            width: 100%;
            max-width: 800px;
            margin: 0 auto;
            border: 2px solid #000;
            padding: 20px;
        }

        /* ===== HEADER ===== */
        .header {
            text-align: center;
            border-bottom: 2px solid #000;
            padding-bottom: 12px;
            margin-bottom: 16px;
        }
        .header img {
            height: 100px;
            width: auto;
            margin: 0 auto;
        }
        .header h1 {
            font-size: 22pt;
            font-weight: bold;
            letter-spacing: 2px;
            margin-top: 8px;
        }
        .header h2 {
            font-size: 13pt;
            font-weight: bold;
            margin-top: 4px;
        }
        .header p {
            font-size: 10pt;
            margin-top: 4px;
            color: #333;
        }

        /* ===== INFO FIELDS ===== */
        .info-table {
            width: 100%;
            margin-bottom: 16px;
            border-collapse: collapse;
        }
        .info-table td {
            padding: 4px 0;
            vertical-align: top;
            font-size: 11pt;
        }
        .info-table td.label {
            font-weight: bold;
            width: 130px;
            white-space: nowrap;
        }
        .info-table td.value {
            border-bottom: 1px dotted #666;
            padding-left: 6px;
        }
        .info-table td.gap {
            width: 30px;
        }

        /* ===== ITEMS TABLE ===== */
        .items-table {
            width: 100%;
            border-collapse: collapse;
            border: 2px solid #000;
            margin-bottom: 0;
        }
        .items-table thead th {
            background: #f0f0f0;
            font-weight: bold;
            font-size: 10pt;
            text-align: left;
            padding: 8px;
            border-bottom: 2px solid #000;
            border-right: 1px solid #666;
        }
        .items-table thead th:last-child {
            border-right: none;
        }
        .items-table tbody td {
            padding: 8px;
            border-bottom: 1px solid #ccc;
            border-right: 1px solid #666;
            font-size: 11pt;
            vertical-align: top;
        }
        .items-table tbody td:last-child {
            border-right: none;
        }
        .items-table tbody tr:last-child td {
            border-bottom: 2px solid #000;
        }
        .items-table .col-account { width: 22%; }
        .items-table .col-details { width: 56%; }
        .items-table .col-amount { width: 22%; text-align: right; }

        .items-table tfoot td {
            padding: 10px 8px;
            font-size: 10pt;
            vertical-align: top;
            border-right: 1px solid #666;
        }
        .items-table tfoot td:last-child {
            border-right: none;
        }
        .amount-words-label {
            font-weight: bold;
            font-size: 10pt;
        }
        .amount-words-value {
            font-weight: bold;
            font-size: 11pt;
            margin-top: 4px;
            text-transform: uppercase;
        }
        .total-label {
            font-weight: bold;
            font-size: 12pt;
            text-align: right;
        }
        .total-value {
            font-weight: bold;
            font-size: 14pt;
            text-align: right;
            margin-top: 4px;
        }

        /* ===== SIGNATURES TABLE ===== */
        .signatures-table {
            width: 100%;
            border-collapse: collapse;
            border: 2px solid #000;
            margin-top: 0;
        }
        .signatures-table thead th {
            background: #f0f0f0;
            font-weight: bold;
            font-size: 10pt;
            text-align: left;
            padding: 8px;
            border-bottom: 2px solid #000;
            border-right: 1px solid #666;
        }
        .signatures-table thead th:last-child {
            border-right: none;
        }
        .signatures-table tbody td {
            padding: 12px 8px;
            border-bottom: 1px solid #ccc;
            border-right: 1px solid #666;
            font-size: 11pt;
            vertical-align: bottom;
            height: 50px;
        }
        .signatures-table tbody td:last-child {
            border-right: none;
        }
        .signatures-table tbody tr:last-child td {
            border-bottom: none;
        }
        .sig-label {
            font-weight: bold;
            font-size: 10pt;
        }
        .sig-name {
            font-size: 11pt;
            margin-top: 2px;
        }

        /* ===== RECEIVED BY ===== */
        .received-section {
            margin-top: 16px;
            display: flex;
            justify-content: space-between;
            align-items: flex-end;
            gap: 40px;
        }
        .received-section .field {
            flex: 1;
        }
        .received-section .field .label {
            font-weight: bold;
            font-size: 11pt;
            margin-bottom: 4px;
        }
        .received-section .field .value {
            border-bottom: 1px solid #000;
            height: 30px;
        }
    </style>
</head>
<body>
    <div class="container">

        {{-- ============================================================ --}}
        {{-- HEADER                                                     --}}
        {{-- ============================================================ --}}
        <div class="header">
            <img src="data:image/png;base64,{{ base64_encode(file_get_contents(public_path('images/sapta-logo.png'))) }}" alt="SAPTA">
            <h1>PAYMENT VOUCHER</h1>
            <h2>SOIL-ANIMALS' POWER TANZANIA</h2>
            <p>P.O Box 149, Morogoro, Tanzania &nbsp;|&nbsp; Kihonda - Kilimanjaro</p>
        </div>

        {{-- ============================================================ --}}
        {{-- INFO FIELDS                                                --}}
        {{-- ============================================================ --}}
        <table class="info-table">
            <tr>
                <td class="label">Date:</td>
                <td class="value">{{ $voucher->payment_date ? \Carbon\Carbon::parse($voucher->payment_date)->format('d/m/Y') : '—' }}</td>
                <td class="gap"></td>
                <td class="label">Trans No:</td>
                <td class="value"><strong>{{ $voucher->trans_no ?? $voucher->voucher_number }}</strong></td>
            </tr>
            <tr>
                <td class="label">Name of Payee:</td>
                <td class="value">{{ $voucher->payee_name }}</td>
                <td class="gap"></td>
                <td class="label">P.O Box:</td>
                <td class="value">{{ $voucher->payee_pobox ?? '—' }}</td>
            </tr>
            <tr>
                <td class="label">Batch:</td>
                <td class="value">{{ $voucher->batch ?? '—' }}</td>
                <td class="gap"></td>
                <td class="label">Bank:</td>
                <td class="value">{{ $voucher->bank ?? '—' }}</td>
            </tr>
            <tr>
                <td class="label">Currency:</td>
                <td class="value">{{ $voucher->currency ?? 'TZS' }}</td>
                <td class="gap"></td>
                <td class="label">Mode:</td>
                <td class="value">{{ strtoupper($voucher->mode ?? $voucher->payment_method ?? '—') }}</td>
            </tr>
            <tr>
                <td class="label">Cheque Number:</td>
                <td class="value">{{ $voucher->cheque_number ?? '—' }}</td>
                <td class="gap"></td>
                <td class="label"></td>
                <td class="value"></td>
            </tr>
        </table>

        {{-- ============================================================ --}}
        {{-- ITEMS TABLE                                                --}}
        {{-- ============================================================ --}}
        <table class="items-table">
            <thead>
                <tr>
                    <th class="col-account">Account/Invoice No</th>
                    <th class="col-details">Details</th>
                    <th class="col-amount">Amount {{ $voucher->currency ?? 'TZS' }}</th>
                </tr>
            </thead>
            <tbody>
                @forelse($voucher->items as $item)
                    <tr>
                        <td>{{ $item->account_invoice_no ?? '—' }}</td>
                        <td>{{ $item->details }}</td>
                        <td style="text-align: right;">{{ number_format($item->amount, 2) }}</td>
                    </tr>
                @empty
                    <tr>
                        <td colspan="3" style="text-align: center; color: #999;">No items</td>
                    </tr>
                @endforelse
            </tbody>
            <tfoot>
                <tr>
                    <td colspan="2" style="border-top: 2px solid #000;">
                        <div class="amount-words-label">Amount in Words:</div>
                        <div class="amount-words-value">{{ $voucher->amount_in_words ?? '—' }}</div>
                    </td>
                    <td style="border-top: 2px solid #000; text-align: right;">
                        <div class="total-label">TOTAL</div>
                        <div class="total-value">{{ number_format($voucher->amount, 2) }}</div>
                    </td>
                </tr>
            </tfoot>
        </table>

        {{-- ============================================================ --}}
        {{-- SIGNATURES TABLE                                           --}}
        {{-- ============================================================ --}}
        <table class="signatures-table">
            <thead>
                <tr>
                    <th style="width: 40%;">Name</th>
                    <th style="width: 35%;">Signature</th>
                    <th style="width: 25%;">Date</th>
                </tr>
            </thead>
            <tbody>
                <tr>
                    <td>
                        <div class="sig-label">Prepared By:</div>
                        <div class="sig-name">{{ $voucher->preparedBy->username ?? '—' }}</div>
                    </td>
                    <td></td>
                    <td>{{ $voucher->prepared_at ? \Carbon\Carbon::parse($voucher->prepared_at)->format('d/m/Y') : '' }}</td>
                </tr>
                <tr>
                    <td>
                        <div class="sig-label">Checked By:</div>
                        <div class="sig-name">{{ $voucher->checkedBy->username ?? '—' }}</div>
                    </td>
                    <td></td>
                    <td>{{ $voucher->checked_at ? \Carbon\Carbon::parse($voucher->checked_at)->format('d/m/Y') : '' }}</td>
                </tr>
                <tr>
                    <td>
                        <div class="sig-label">Authorized By:</div>
                        <div class="sig-name">{{ $voucher->authorizedBy->username ?? '—' }}</div>
                    </td>
                    <td></td>
                    <td>{{ $voucher->authorized_at ? \Carbon\Carbon::parse($voucher->authorized_at)->format('d/m/Y') : '' }}</td>
                </tr>
            </tbody>
        </table>

        {{-- ============================================================ --}}
        {{-- RECEIVED BY                                                --}}
        {{-- ============================================================ --}}
        <div class="received-section">
            <div class="field">
                <div class="label">Received BY:</div>
                <div class="value">{{ $voucher->received_by_name ?? '' }}</div>
            </div>
            <div class="field">
                <div class="label">Signature:</div>
                <div class="value"></div>
            </div>
        </div>

    </div>
</body>
</html>