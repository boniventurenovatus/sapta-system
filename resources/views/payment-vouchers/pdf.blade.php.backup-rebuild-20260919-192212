<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <title>Payment Voucher {{ $voucher->trans_no ?? $voucher->voucher_number }}</title>
    <style>
        @page { size: A4; margin: 0; }
        * { margin: 0; padding: 0; box-sizing: border-box; }
        body { font-family: Arial, Helvetica, sans-serif; font-size: 10pt; color: #000; line-height: 1.3; margin: 0; padding: 0; }
        .container { width: 100%; border: 2px solid #000; padding: 10mm 8mm; box-sizing: border-box; }

        /* HEADER */
        .header { text-align: center; border-bottom: 2px solid #000; padding-bottom: 6px; margin-bottom: 10px; }
        .header .title { font-size: 18pt; font-weight: bold; letter-spacing: 3px; margin-bottom: 4px; }
        .header .org { font-size: 11pt; font-weight: bold; margin-bottom: 2px; }
        .header .address { font-size: 9pt; margin-bottom: 6px; }
        .header img { height: 70px; width: auto; display: block; margin: 0 auto; }

        /* INFO */
        .info-table { width: 100%; margin-bottom: 12px; border-collapse: collapse; }
        .info-table td { padding: 3px 0; vertical-align: top; font-size: 10pt; }
        .info-table td.label { font-weight: bold; white-space: nowrap; padding-right: 4px; }
        .info-table td.value { border-bottom: 1px dotted #666; padding-left: 4px; padding-right: 12px; min-width: 140px; }

        /* ITEMS TABLE */
        .items-table { width: 100%; border-collapse: collapse; border: 2px solid #000; }
        .items-table thead th { background: #f0f0f0; font-weight: bold; font-size: 10pt; text-align: left; padding: 6px 8px; border-bottom: 2px solid #000; border-right: 1px solid #666; }
        .items-table thead th:last-child { border-right: none; text-align: right; }
        .items-table tbody td { padding: 6px 8px; border-bottom: 1px solid #ccc; border-right: 1px solid #666; font-size: 10pt; vertical-align: top; min-height: 40px; }
        .items-table tbody td:last-child { border-right: none; text-align: right; }
        .items-table .col-account { width: 22%; }
        .items-table .col-details { width: 56%; }
        .items-table .col-amount { width: 22%; text-align: right; }
        .items-table tfoot td { padding: 8px; font-size: 10pt; vertical-align: top; border-right: 1px solid #666; border-top: 2px solid #000; }
        .items-table tfoot td:last-child { border-right: none; text-align: right; }
        .amount-words-label { font-weight: bold; font-size: 10pt; margin-bottom: 2px; }
        .amount-words-value { font-size: 10pt; text-transform: capitalize; }
        .total-label { font-weight: bold; font-size: 11pt; margin-bottom: 2px; }
        .total-value { font-weight: bold; font-size: 14pt; }

        /* SIGNATURES */
        .signatures-table { width: 100%; border-collapse: collapse; border: 2px solid #000; border-top: none; }
        .signatures-table thead th { background: #f0f0f0; font-weight: bold; font-size: 10pt; text-align: left; padding: 6px 8px; border-bottom: 2px solid #000; border-right: 1px solid #666; }
        .signatures-table thead th:last-child { border-right: none; }
        .signatures-table tbody td { padding: 10px 8px; border-bottom: 1px solid #ccc; border-right: 1px solid #666; font-size: 10pt; vertical-align: top; height: 48px; }
        .signatures-table tbody td:last-child { border-right: none; }
        .signatures-table tbody tr:last-child td { border-bottom: none; }
        .sig-label { font-weight: bold; font-size: 9pt; }
        .sig-name { font-size: 10pt; margin-top: 2px; }

        /* RECEIVED BY */
        .received-section { margin-top: 12px; width: 100%; border-collapse: collapse; }
        .received-section td { padding: 4px 0; vertical-align: bottom; font-size: 10pt; }
        .received-section .label { font-weight: bold; white-space: nowrap; padding-right: 6px; }
        .received-section .value { border-bottom: 1px solid #000; height: 22px; padding-left: 4px; }
        .received-section .gap { width: 30px; }
    </style>
</head>
<body style="margin:0; padding:0;">
    <div class="container" style="padding: 10mm 8mm; box-sizing: border-box;">

        {{-- HEADER --}}
        <div class="header">
            <div class="title">PAYMENT VOUCHER</div>
            <div class="org">SOIL- ANIMALS' POWER TANZANIA</div>
            <div class="address">P.O Box 149, Morogoro, Tanzania Kihonda- Kilimanjaro</div>
            <img src="data:image/png;base64,{{ base64_encode(file_get_contents(public_path('images/sapta-logo.png'))) }}" alt="SAPTA">
        </div>

        {{-- INFO --}}
        <table class="info-table">
            <tr>
                <td class="label">Date:</td>
                <td class="value">{{ $voucher->payment_date ? \Carbon\Carbon::parse($voucher->payment_date)->format('d/m/Y') : '—' }}</td>
                <td class="label" style="padding-left:20px;">Trans No:</td>
                <td class="value"><strong>{{ $voucher->trans_no ?? $voucher->voucher_number }}</strong></td>
            </tr>
            <tr>
                <td class="label">Name of Payee:</td>
                <td class="value">{{ $voucher->payee_name }}</td>
                <td class="label" style="padding-left:20px;">P.O Box:</td>
                <td class="value">{{ $voucher->payee_pobox ?? '—' }}</td>
            </tr>
            <tr>
                <td class="label">Batch:</td>
                <td class="value">{{ $voucher->batch ?? '—' }}</td>
                <td class="label">Bank:</td>
                <td class="value">{{ $voucher->bank ?? '—' }}</td>
                <td class="label">Currency:</td>
                <td class="value">{{ $voucher->currency ?? 'TZS' }}</td>
            </tr>
            <tr>
                <td class="label">Mode:</td>
                <td class="value">{{ strtoupper($voucher->mode ?? $voucher->payment_method ?? '—') }}</td>
                <td class="label" style="padding-left:20px;">Cheque Number:</td>
                <td class="value">{{ $voucher->cheque_number ?? '—' }}</td>
            </tr>
        </table>

        {{-- ITEMS --}}
        <table class="items-table">
            <thead>
                <tr>
                    <th class="col-account">Account/Invoice No</th>
                    <th class="col-details">Details</th>
                    <th class="col-amount">Amount {{ $voucher->currency ?? 'TZS' }}</th>
                </tr>
            </thead>
            <tbody>
                @forelse($voucher->items as $item)
                    <tr>
                        <td>{{ $item->account_invoice_no ?? '—' }}</td>
                        <td>{{ $item->details }}</td>
                        <td style="text-align: right;">{{ number_format($item->amount, 2) }}</td>
                    </tr>
                @empty
                    <tr><td colspan="3" style="text-align: center; color: #999;">No items</td></tr>
                @endforelse
            </tbody>
            <tfoot>
                <tr>
                    <td colspan="2">
                        <div class="amount-words-label">Amount in Words:</div>
                        <div class="amount-words-value">{{ $voucher->amount_in_words ?? '—' }}</div>
                    </td>
                    <td>
                        <div class="total-label">TOTAL</div>
                        <div class="total-value">{{ number_format($voucher->amount, 2) }}</div>
                    </td>
                </tr>
            </tfoot>
        </table>

        {{-- SIGNATURES --}}
        <table class="signatures-table">
            <thead>
                <tr>
                    <th style="width: 40%;">Name</th>
                    <th style="width: 35%;">Signature</th>
                    <th style="width: 25%;">Date</th>
                </tr>
            </thead>
            <tbody>
                <tr>
                    <td><div class="sig-label">Prepared By:</div><div class="sig-name">{{ $voucher->preparedBy->username ?? '—' }}</div></td>
                    <td></td>
                    <td>{{ $voucher->prepared_at ? \Carbon\Carbon::parse($voucher->prepared_at)->format('d/m/Y') : '' }}</td>
                </tr>
                <tr>
                    <td><div class="sig-label">Checked By:</div><div class="sig-name">{{ $voucher->checkedBy->username ?? '—' }}</div></td>
                    <td></td>
                    <td>{{ $voucher->checked_at ? \Carbon\Carbon::parse($voucher->checked_at)->format('d/m/Y') : '' }}</td>
                </tr>
                <tr>
                    <td><div class="sig-label">Authorized By:</div><div class="sig-name">{{ $voucher->authorizedBy->username ?? '—' }}</div></td>
                    <td></td>
                    <td>{{ $voucher->authorized_at ? \Carbon\Carbon::parse($voucher->authorized_at)->format('d/m/Y') : '' }}</td>
                </tr>
            </tbody>
        </table>

        {{-- RECEIVED BY --}}
        <table class="received-section">
            <tr>
                <td class="label">Received BY:</td>
                <td class="value">{{ $voucher->received_by_name ?? '' }}</td>
                <td class="gap"></td>
                <td class="label">Signature:</td>
                <td class="value"></td>
            </tr>
        </table>

    </div>
</body>
</html>